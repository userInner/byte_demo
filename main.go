package main

func main() {
	// r := gin.Default()
	// common.InitMysql()
	// common.InitMinio()
	// routers.InitRouter(r)
	// err := r.Run() // 使用gin默认端口 8080
	// if err != nil {
	// 	panic(err)
	// }
}
